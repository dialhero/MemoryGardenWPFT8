namespace MemoryGardenWPF.Models
{
    public enum ThemeKey { Animals, Fruits, Shapes }

    public enum BoardSize
    {
        Tiny,   // 2x3 -> 3 pairs
        Easy,   // 3x4 -> 6 pairs
        Medium  // 4x4 -> 8 pairs
    }
}
